<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.ciudadania360</groupId>
  <artifactId>ciudadania360</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>ciudadania360</name>

  <modules>
    <module>shared</module>
    <module>subsistema-ciudadano</module>
    <module>subsistema-informacion</module>
    <module>subsistema-comunicaciones</module>
    <module>subsistema-tramitacion</module>
    <module>subsistema-videoconferencia</module>
    <module>gestion-roles-permisos</module>
    <module>subsistema-ia</module>
    <module>subsistema-interno</module>
    <module>aywebfwk-backend</module>
    <module>ciudadania360-app</module>
  </modules>

  <properties>
    <java.version>17</java.version>
    <spring-boot.version>3.3.3</spring-boot.version>

    <flyway-core.version>10.20.1</flyway-core.version>
    <flyway-database-postgresql.version>10.20.1</flyway-database-postgresql.version>

    <!-- Versiones comunes -->
    <mapstruct.version>1.5.5.Final</mapstruct.version>
    <lombok.version>1.18.32</lombok.version>
    <springdoc.version>2.5.0</springdoc.version>
    <swagger-core.version>2.2.22</swagger-core.version>

    <!-- Versiones AyWebFwk -->
    <cxf.version>4.0.3</cxf.version>
    <jaxb.version>2.3.1</jaxb.version>
    <hibernate.version>5.6.15.Final</hibernate.version>
    <jasperreports.version>6.20.0</jasperreports.version>
    
    <!-- Versiones del Ayuntamiento -->
    <gca.version>1.0.0</gca.version>
    <ayhost.version>1.0.0</ayhost.version>
    <aywebfwk.version>1.0.0</aywebfwk.version>

    <maven.compiler.parameters>true</maven.compiler.parameters>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Spring Boot BOM -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Flyway -->
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-core</artifactId>
        <version>${flyway-core.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-database-postgresql</artifactId>
        <version>${flyway-database-postgresql.version}</version>
      </dependency>

      <!-- MapStruct API + Processor -->
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct</artifactId>
        <version>${mapstruct.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct-processor</artifactId>
        <version>${mapstruct.version}</version>
      </dependency>

      <!-- Lombok -->
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>${lombok.version}</version>
      </dependency>

      <!-- Springdoc OpenAPI -->
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc.version}</version>
      </dependency>

      <!-- Swagger Core (para usar io.swagger.v3.oas.models.*) -->
      <dependency>
        <groupId>io.swagger.core.v3</groupId>
        <artifactId>swagger-core</artifactId>
        <version>${swagger-core.version}</version>
      </dependency>

      <!-- Shared interno -->
      <dependency>
        <groupId>com.ciudadania360</groupId>
        <artifactId>shared</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.swagger.core.v3</groupId>
        <artifactId>swagger-models</artifactId>
        <version>${swagger-core.version}</version>
      </dependency>

      <!-- ===== LOG4J2 ===== -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-log4j2</artifactId>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-api</artifactId>
        <version>2.21.0</version>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-core</artifactId>
        <version>2.21.0</version>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-slf4j2-impl</artifactId>
        <version>2.21.0</version>
      </dependency>
      <!-- ================= -->

      <!-- ===== AYWEBFWK ===== -->
      <!-- AyWebFwk Framework -->
      <!-- TODO: Disponible solo en repositorio del Ayuntamiento -->
      <!-- <dependency>
        <groupId>es.valencia.aywebfwk</groupId>
        <artifactId>aywebfwk-core</artifactId>
        <version>${aywebfwk.version}</version>
      </dependency> -->
      
      <!-- CXF para servicios web -->
      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-frontend-jaxws</artifactId>
        <version>${cxf.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-rt-transports-http</artifactId>
        <version>${cxf.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.cxf</groupId>
        <artifactId>cxf-spring-boot-starter-jaxws</artifactId>
        <version>${cxf.version}</version>
      </dependency>
      
      <!-- Hibernate -->
      <dependency>
        <groupId>org.hibernate</groupId>
        <artifactId>hibernate-core</artifactId>
        <version>${hibernate.version}</version>
      </dependency>
      
      <!-- JAXB -->
      <dependency>
        <groupId>javax.xml.bind</groupId>
        <artifactId>jaxb-api</artifactId>
        <version>${jaxb.version}</version>
      </dependency>
      
      <!-- GCA (Gestión de Control de Accesos) - TODO: Disponible solo en repositorio del Ayuntamiento -->
      <!-- <dependency>
        <groupId>es.valencia.gca</groupId>
        <artifactId>gca-core</artifactId>
        <version>${gca.version}</version>
      </dependency> -->
      
      <!-- AyHost para comunicación con host - TODO: Disponible solo en repositorio del Ayuntamiento -->
      <!-- <dependency>
        <groupId>es.valencia.ayhost</groupId>
        <artifactId>ayhost</artifactId>
        <version>${ayhost.version}</version>
      </dependency> -->
      
      <!-- JasperReports -->
      <dependency>
        <groupId>net.sf.jasperreports</groupId>
        <artifactId>jasperreports</artifactId>
        <version>${jasperreports.version}</version>
      </dependency>
      <!-- =================== -->
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <!-- Compilador con annotation processors: MapStruct + Lombok -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
          <parameters>true</parameters>
          <annotationProcessorPaths>
            <path>
              <groupId>org.mapstruct</groupId>
              <artifactId>mapstruct-processor</artifactId>
              <version>${mapstruct.version}</version>
            </path>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok-mapstruct-binding</artifactId>
              <version>0.2.0</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>

      <!-- Jacoco -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.10</version>
        <executions>
          <execution>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
