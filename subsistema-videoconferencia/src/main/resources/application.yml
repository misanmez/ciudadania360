server:
  port: 8086
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ciudadania360
    username: postgres
    password: Aqmdla04
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: videoconferencia
    open-in-view: false
  flyway:
    enabled: true
    schemas: videoconferencia
    locations: classpath:db/migration
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth.local/realms/ciudadania360
management:
  endpoints:
    web:
      exposure:
        include: health,info
